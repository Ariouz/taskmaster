[X] - Structure projet => Makefile, classes générales, utils
[X] - Lecture config et init

Gestion process:
[X] - Boucle processes => umask(int) + fork + chdir(workdir config) + setenv
[X] - dup2 des stderr/out ou /dev/null si non spécifiés
[X] - check si le binaire de la cmd existe => execve
[X] - parent => stocker pid, start, status => process

Boucle monitoring (Thread séparé while + sleep):
    [X] - waitpid(pid, &status, WNOHAND) => detection process mort + log => > 0 = mort: WIFEXITED, WEXITSTATUS (comparer avec exit_codes), WIFSIGNALED
    [X] - check si time-start_time > X secondes => status Started
    [X] - Maj status process + log
    Autorestart:
        [X] - check autorestart pour le process fail
        [X] - Retry jusqu'au max_retries + log

Signaux:
[] - Gestion sig SIGHUP (reload config)

Gestion des logs:
[X] => Logger (global)
[X] => Redirection des sorties std vers fd pour les process => dup2 + fichier (def dans config)
[] - Shell interactif => lib readline => status de process, start/stop/restart, reload, quit 

BONUS:
[X] => Logging syslog
[X] => Colorized logs, status
[X] => Commande logs pour attacher les logs: verbose true, pas de readline, ctrl+c pour quitter le logs et retourner au shell, ctrl+c again = rien
[x] => Status avec args
[] => Shell/Server séparés
[] => Attach/detach